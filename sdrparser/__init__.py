"""
SDRParser — processes raw audio from SDR++ and parses DMR, NXDN, and P25
digital voice frames, outputting formatted headers and MBE codec frames in
both interleaved (as-transmitted) and deinterleaved (codec-ready) form.
"""

__version__ = "0.1.0"
